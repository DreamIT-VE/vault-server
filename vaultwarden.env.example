# Example of the runtime .env that the GitHub Action will create on the server
# Do NOT store secrets in the repository. Use GitHub Secrets instead.

# Domain used by Vaultwarden (must be https):
DOMAIN=https://vault.dreamit.software

# Postgres database credentials used by the PostgreSQL container
POSTGRES_USER=vaultwarden
POSTGRES_PASSWORD=super_secure_password
POSTGRES_DB=vaultwarden
DATABASE_URL=postgresql://vaultwarden:super_secure_password@postgres:5432/vaultwarden

# Admin token for the Vaultwarden Admin UI (Argon2 PHC or plain string):
ADMIN_TOKEN='$argon2id$...'

# Optional SMTP settings for email verification/password reset
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=mail_user
SMTP_PASSWORD=mail_password
SMTP_FROM=vault@your-domain.tld
SMTP_SECURITY=starttls

# Note: the GitHub workflow will write a real .env on the server from GitHub Secrets
# and Docker Compose will read it with `env_file: - .env`.